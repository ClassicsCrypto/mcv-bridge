import { CONFIG, COLLECTION_NAME } from "./config";

export const MARS_CATS_ADDRESS = CONFIG.contracts.marsCats;
export const BEACON_ADDRESS = CONFIG.contracts.beacon;
export const DESTINATION_EID = CONFIG.destinationEid;
export const SOURCE_CHAIN_ID = CONFIG.sourceChain.id;
export const DESTINATION_CHAIN_ID = CONFIG.destinationChain.id;

export { COLLECTION_NAME };
